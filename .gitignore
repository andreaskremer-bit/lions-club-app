node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Playwright
/test-results
/playwright-report
/.playwright

# Claude Code – persönliche, lokale Settings (Permissions, Pfade)
.claude/settings.local.json

# Ad-hoc-Exporte mit Mitgliederdaten (PII) – nie committen
berechtigungen*.csv

# Sitzungs-Protokolle (interne PDFs mit Mitgliederdaten) – nur in die App-Ablage,
# NIE ins öffentliche Repo committen
Protokolle/

# Club-internes Material (Anleitungen, Konzepte, Praesentationen).
# Bleibt lokal — das Repo ist oeffentlich. Die vier zuvor getrackten Dateien
# wurden per `git rm --cached` entfernt (Inhalt geprueft: keine PII).
docs/
